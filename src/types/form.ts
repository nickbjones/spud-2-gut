// maybe this isn't needed?
export type FormData = {
  id: string;
  uid: string;
  title: string;
  tags: string[];
  date: string;
  description: string;
  ingredients: string;
  instructions: string;
  reference: string;
};
